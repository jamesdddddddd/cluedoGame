using UnityEngine;

public class Deck : MonoBehaviour
{
    // we need 6 characrters, 9 rooms, 6 weapons


    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
