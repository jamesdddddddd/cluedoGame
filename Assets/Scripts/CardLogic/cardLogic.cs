using UnityEngine;

public class cardLogic : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created

    // we need 6 characrters, 9 rooms, 6 weapons
    // we will need a sheet of paper to wite down cards that you have, and you found out from other players.

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
